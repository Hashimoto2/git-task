<div class="body">
  <div class="container">
    <div class="row">
	    <div class="col-md-6 col-md-offset-3">
		    <h1>Editing Book</h1>
		    <%= form_with(model: @book, local: true) do |f| %>
        <% if @book.errors.any? %>
        <div class="notifications">
          <div class="notice"><%= @book.errors.count %>errors prohibited this obj from being saved:</div>
        </div>
        <div class="alert alert-warning">
          <ul>
            <% @book.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
        <% end %>
        <h4 class="title1">Title</h4>
        <%= f.text_field :title, class: "title12" %>
        <h4 class="opinion1">Opinion</h4>
        <%= f.text_area :opinion, class: "opinion12" %>
        <%= f.submit 'Update Book', class: "box3"%>
        <% end %>
	      <%= link_to "Show", book_path(@book.id) %>
	      |
        <%= link_to "Back", books_path %>
      </div>
    </div>
  </div>
</div>
